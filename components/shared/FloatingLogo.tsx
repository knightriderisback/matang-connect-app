"use client";

/**
 * Floating history shortcut was retired to prevent blocking bottom-right
 * interactive card elements (like WhatsApp share and card actions).
 * Users can access History via the primary Logo in AppHeader.
 */
export function FloatingLogo() {
  return null;
}

export default FloatingLogo;
